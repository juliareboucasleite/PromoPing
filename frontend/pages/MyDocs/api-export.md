# Exportação

Documentação dos endpoints de exportação de dados.

```yaml
type: builtin:openapi
props:
  tag: Exportação
  models: false
dependencies:
  spec:
    ref:
      kind: openapi
      spec: my-api-promoping
```

