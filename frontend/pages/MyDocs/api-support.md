# Suporte API

Documentação dos endpoints do sistema de suporte ao cliente.

```yaml
type: builtin:openapi
props:
  tag: Suporte
  models: false
dependencies:
  spec:
    ref:
      kind: openapi
      spec: my-api-promoping
```

