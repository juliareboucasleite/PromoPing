# Charts

Documentação dos endpoints de dados para gráficos e estatísticas.

```yaml
type: builtin:openapi
props:
  tag: Charts
  models: false
dependencies:
  spec:
    ref:
      kind: openapi
      spec: my-api-promoping
```

