# Autenticação

Documentação dos endpoints de autenticação da API PromoPing.

```yaml
type: builtin:openapi
props:
  tag: Autenticação
  models: false
dependencies:
  spec:
    ref:
      kind: openapi
      spec: my-api-promoping
```

