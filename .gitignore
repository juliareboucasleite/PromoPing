# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
*.log
logs/

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage directory used by tools like istanbul
coverage/

# nyc test coverage
.nyc_output

# Dependency directories
jspm_packages/

# Optional npm cache directory
.npm

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variables file
.env

# Project specific
discord-users.json
*.sql
*.mwb
node_modules/.bin/eslint
node_modules/.bin/eslint

docs
src/generated
**/*.min.js

#  Sensíveis – não publicar no GitHub
# Variáveis de ambiente (em qualquer pasta: backend, python-scraper, etc.)
.env
.env.*
**/.env
**/.env.*

# Configuração Discord (tokens, client secret)
config-files/discord-config.js

# Especificação da API (endpoints, URLs de produção, estrutura interna)
openapi.yaml

# Config de produção (URLs, basePath por ambiente)
frontend/.production-config.json

# Ficheiros com chaves/segredos de API (ex.: api-keys.json, secrets.json)
*api*key*
*secret*.json
*credentials*
*-credentials.*

# Chaves e certificados privados
*.pem
*.key
!*.pub

# Backups de base de dados (podem conter dados reais)
*.sql.gz
*.sql.backup
dump*.sql
python-scraper/__pycache__/scraper.cpython-313.pyc

# Deploy e Docker – não publicar no GitHub
deploy-files/
docker-files/

# Configuração Herald (CI/linting)
herald.config.json
